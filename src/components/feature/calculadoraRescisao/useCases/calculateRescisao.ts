export const calculateRescisao = ({
  salaryGross,
  startDate,
  endDate,
  reasonType,
  noticeType,
  dependentsNumber = 0,
}: RescisaoInputs): RescisaoResult => {
  return {} as RescisaoResult;
};
